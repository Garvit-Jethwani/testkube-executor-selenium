apiVersion: executor.testkube.io/v1
kind: Executor
metadata:
  name: go-test-executor
  namespace: testkube
spec:
  image: elithepoorboy/go-test:latest  # pass your repository and tag
  types:
  - go/test
  # volume_mount_path: /var/tmp/Roost/ebsvolume/testkube-minio
  # volume_quantity: 10Gix
