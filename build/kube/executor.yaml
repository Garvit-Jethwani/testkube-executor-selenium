apiVersion: executor.testkube.io/v1
kind: Executor
metadata:
  name: selenium-test-executor
  namespace: testkube
spec:
  image: elithepoorboy/selenium:check3  # pass your repository and tag
  types:
  - selenium/test
  # volume_mount_path: /var/tmp/Roost/ebsvolume/testkube-minio
  # volume_quantity: 10Gix
